{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "bunx prisma generate --schema=../../packages/db/prisma/schema.prisma && next build",
  "crons": [
    {
      "path": "/api/cron/graph-subscription",
      "schedule": "0 * * * *"
    }
  ]
}
